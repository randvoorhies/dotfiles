if [[ `uname` == 'Linux' ]]; then
  alias ls='ls --color'
fi

alias axel='axel -a'
alias sprunge='curl -F "sprunge=<-" http://sprunge.us'
alias vim='vim -p'
alias gvim='gvim -p'
alias grep='grep --color -n'
# vim:syntax=sh
