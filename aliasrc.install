if [[ `uname` == 'Linux' ]]; then
  alias ls='ls --color'
fi

alias axel='axel -a'
alias sprunge='curl -F "sprunge=<-" http://sprunge.us'
alias cgrep='grep --color -n'

alias matlab='matlab -nodesktop'
# vim:syntax=sh
