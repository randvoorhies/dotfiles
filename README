The following dotfiles are for keeping Sagar's workspaces consistent.  The easiest way to use them is to clone the repo anywhere on your machine, and then run setup.py. The setup.py script will make appropriate simlinks to all of the included dotfiles, and will make backups of any files it is overwriting.
