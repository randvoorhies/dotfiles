# source my bashrc
if [ -f "$HOME/.bashrc" ];
then
  source "$HOME/.bashrc"
fi

# source hostname specific configs (this is where the MOTD lives)
if [ -f "$HOME/.$(hostname)" ]; then
  source "$HOME/.$(hostname)"
fi
